.user_window {
	.user_avatar{
		img.avatar { height: 250px; margin-top: 30px; }
	}
}

.each_repo {
	p { font-size: 14px; font-weight: 400; text-align: center; margin: 0; }
	background-color: rgba(255,255,255,0.5);
	padding: 5px;
}